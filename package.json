{
  "name": "quietpatch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node --check src/app.js && node --check src/analyzer.js && node --check scripts/build.mjs",
    "test": "node --test",
    "validate": "npm run check && npm test && npm run build"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}

